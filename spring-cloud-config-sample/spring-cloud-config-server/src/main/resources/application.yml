server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/haedoang/spring-cloud-sample-config-repo
          clone-on-start: true
          default-label: master
  security:
    user:
      name: root
      password: 1234